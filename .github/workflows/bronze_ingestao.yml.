name: "Teste de Vida do Actions"

on:
  push:
  workflow_dispatch:

jobs:
  teste-simples:
    runs-on: ubuntu-latest
    steps:
      - name: Dizer Olá
        run: echo "🚀 O GitHub Actions está vivo e a funcionar perfeitamente!"

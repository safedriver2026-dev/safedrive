name: SafeDriver Atualização Diaria
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
jobs:
  pipeline:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      - name: Instalar Dependencias
        run: pip install -r requirements.txt
      - name: Rodar Motor
        env:
          DISCORD_SUCESSO: ${{ secrets.DISCORD_SUCESSO }}
          DISCORD_ERRO: ${{ secrets.DISCORD_ERRO }}
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          python autobot/motor_safedriver.py
          python gerador_diagramas.py
      - name: Auditoria Pytest
        run: python -m pytest tests/test_seguranca.py
      - name: Git Push
        run: |
          git config --local user.email "bot@safedriver.com"
          git config --local user.name "SafeDriver Bot"
          git add datalake/auditoria/*.json datalake/ouro/*.csv datalake/ouro/*.parquet documentacao/*.png
          git commit -m "auto: sync auditado [skip ci]" || echo "nada para subir"
          git push

name: "SafeDriver: Pipeline de Produção"

on:
  workflow_dispatch:
    inputs:
      job:
        description: 'Selecione o Job para execução:'
        required: true
        default: 'tudo'
        type: choice
        options:
          - 'BRONZE_CRIMES'
          - 'BRONZE_COMPLETA'
          - 'PRATA_CRIMES'
          - 'PRATA_MAPA'
          - 'PRATA_COMPLETA'
          - 'OURO_COMPLETA'
          - 'FLUXO_CRIMES'
          - 'FLUXO_MAPA'
          - 'tudo'

jobs:
  # --- CAMADA BRONZE: Ingestão Pura ---
  camada-bronze:
    if: |
      github.event.inputs.job == 'tudo' || 
      startsWith(github.event.inputs.job, 'BRONZE_') ||
      startsWith(github.event.inputs.job, 'FLUXO_')
    runs-on: ubuntu-latest
    env:
      R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
      R2_ENDPOINT_URL: ${{ secrets.R2_ENDPOINT_URL }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
    steps:
      - name: 🔍 Checkout do Código
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: "📦 [BRONZE-B] Crimes SSP (Web -> R2)"
        if: github.event.inputs.job == 'tudo' || github.event.inputs.job == 'BRONZE_COMPLETA' || github.event.inputs.job == 'BRONZE_CRIMES' || github.event.inputs.job == 'FLUXO_CRIMES'
        run: |
          # Adicionado pacotes necessários para o script do ingestor não falhar na importação
          pip install boto3 requests polars h3 python-calamine fastexcel
          python autobot/reg_crimes/ingestor_safe_driver.py bronze

  # --- CAMADA PRATA: Transformação e Auditoria ---
  camada-prata:
    needs: [camada-bronze] 
    if: |
      always() && 
      (needs.camada-bronze.result == 'success' || needs.camada-bronze.result == 'skipped') &&
      (github.event.inputs.job == 'tudo' || startsWith(github.event.inputs.job, 'PRATA_') || startsWith(github.event.inputs.job, 'FLUXO_'))
    runs-on: ubuntu-latest
    env:
      R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
      R2_ENDPOINT_URL: ${{ secrets.R2_ENDPOINT_URL }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      BQ_PROJECT_ID: ${{ secrets.BQ_PROJECT_ID }}
      LGPD_PEPPER: ${{ secrets.LGPD_PEPPER }}
    steps:
      - name: 🔍 Checkout do Código
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: "⚙️ Instalar Motores de Performance"
        run: |
          pip install boto3 polars h3 pyarrow requests duckdb python-calamine fastexcel

      - name: "🛡️ [PRATA-A] Processamento Crimes e LGPD"
        if: github.event.inputs.job == 'tudo' || github.event.inputs.job == 'PRATA_COMPLETA' || github.event.inputs.job == 'PRATA_CRIMES' || github.event.inputs.job == 'FLUXO_CRIMES'
        run: python autobot/reg_crimes/ingestor_safe_driver.py prata

      - name: "🏗️ [PRATA-B] Malha Geo & Infra (Híbrido DuckDB/Polars)"
        if: github.event.inputs.job == 'tudo' || github.event.inputs.job == 'PRATA_COMPLETA' || github.event.inputs.job == 'PRATA_MAPA' || github.event.inputs.job == 'FLUXO_MAPA'
        run: |
          echo "🚀 Iniciando motor híbrido..."
          python autobot/prata/malha_geografica/malha_geo_infra_social.py
          
          # Validador de Auditoria (Ajustado para o novo JSON de Auditoria)
          if [ -f "./data_prata/AUDITORIA_PRATA.json" ]; then
            if grep -q '"QUALIDADE": "FALHA"' ./data_prata/AUDITORIA_PRATA.json; then
              echo "❌ ERRO CRÍTICO NA PRATA. PARANDO PIPELINE."
              cat ./data_prata/AUDITORIA_PRATA.json
              exit 1
            fi
          fi

  # --- CAMADA OURO: Tabela Base Analítica (ABT) ---
  camada-ouro:
    needs: [camada-prata]
    if: |
      always() && 
      (needs.camada-prata.result == 'success' || needs.camada-prata.result == 'skipped') &&
      (github.event.inputs.job == 'tudo' || github.event.inputs.job == 'OURO_COMPLETA')
    runs-on: ubuntu-latest
    env:
      R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
      R2_ENDPOINT_URL: ${{ secrets.R2_ENDPOINT_URL }}
      R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
    steps:
      - name: 🔍 Checkout do Código
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: "⚙️ Instalar Bibliotecas Ouro"
        run: pip install boto3 polars pyarrow

      - name: "🏆 [OURO] Processador da Tabela Analítica (ABT)"
        run: python autobot/ouro/processador_ouro_safedriver.py

  # --- NOTIFICAÇÃO ---
  notificacao:
    needs: [camada-bronze, camada-prata, camada-ouro]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: "📢 Feedback para o Discord"
        run: |
          BRONZE_RESULT="${{ needs.camada-bronze.result }}"
          PRATA_RESULT="${{ needs.camada-prata.result }}"
          OURO_RESULT="${{ needs.camada-ouro.result }}"
          
          if [[ ("$BRONZE_RESULT" == "success" || "$BRONZE_RESULT" == "skipped") && \
                ("$PRATA_RESULT" == "success" || "$PRATA_RESULT" == "skipped") && \
                ("$OURO_RESULT" == "success" || "$OURO_RESULT" == "skipped") ]]; then
            URL="${{ secrets.DISCORD_SUCESSO }}"
            MSG="✅ **[SafeDriver]** Pipeline finalizado com sucesso!\nJob: \`${{ github.event.inputs.job }}\`"
          else
            URL="${{ secrets.DISCORD_ERRO }}"
            MSG="🚨 **[SafeDriver]** Falha detectada no pipeline!\nVerifique os logs no GitHub. Job: \`${{ github.event.inputs.job }}\`"
          fi
          
          curl -s -H "Content-Type: application/json" -d "{\"content\": \"$MSG\"}" $URL
